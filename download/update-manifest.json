{
  "versionCode": 72,
  "versionName": "0.68.1",
  "apkUrl": "https://raw.githubusercontent.com/GrapseeAgency/the-gs-ai-app/main/download/GS-AI-App.apk",
  "notes": "v0.68.1 — honest failures + GS Free live: send failures now show their real class (network / server / throttled) and surface the server's own error text instead of a generic label; emulator-loopback literals purged from the build. Server-side: GS Free routing is LIVE — ordinary chat and web-search synthesis are served by the OpenRouter free model pool behind a 14-key self-healing vault, with transparent fallback to the primary provider. One assistant, zero model pickers — routing stays automatic. Still from 0.68.0: the Model Centre, model picker and every model name are gone from the app (binary-verified); explicit search commands always search."
}
