# GS BENCH — CORE requirements (the knowledge-suite execution set).
# Everything here is REQUIRED by cli/main.py adapters at eval time.
#
# SPLIT RATIONALE (raw evidence: failed builds 36031728363, 36032367867,
# 36034869697): a single resolver pass over all harnesses is
# ResolutionImpossible — harbor pins litellm>=1.92.0 vs tau2's
# litellm<1.82.7; crfm-helm drags transformers 5.x/torch against other
# harnesses' pins. So: CORE (this file) installs atomically for eval jobs;
# HEAVY HARNESS EXTRAS install in separate Docker layers
# (requirements-bench-extras.txt) and are irrelevant to the knowledge suite.

inspect-ai>=0.3.42
inspect-evals>=0.21.0   # the inspect_evals/* tasks (gpqa_diamond, aime2024, ifbench)
tau2 @ git+https://github.com/sierra-research/tau2-bench
websockets>=12.0        # tau2 imports it at module load (raw: run 36043125749 tau2 job "No module named 'websockets'")
datasets>=3.0.0
numpy>=1.26.0
scipy>=1.14.0
pyyaml>=6.0
requests>=2.31.0
jsonschema>=4.21.0

# ---- MOVED TO requirements-bench-extras.txt (separate docker layers) ----
# lm-eval>=0.4.0        (not used by knowledge-suite adapters)
# crfm-helm>=0.5.0      (not used; litellm/transformers conflicts)
# swebench>=3.0.0       (suite BLOCKED: 120GB disk > 14GB runner)
# harbor>=0.21.0        (not used; litellm conflict with tau2)
# evalci/abeval/evalstats (stats layer uses local implementations; evalci is
#                          opportunistically imported at runtime when present)
evalci>=0.1.0
